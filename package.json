{
  "name": "davnoot-site",
  "version": "1.0.0",
  "private": true,
  "description": "Davnoot Digital — static marketing site + serverless blog engine.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "comments": {
    "no-build-script": [
      "There is deliberately NO `build` script. Vercel serves this repo as a static",
      "site with zero build step; adding a `build` script would make Vercel start",
      "building and change the output directory resolution. Dependencies here exist",
      "for the /api serverless functions, which Vercel installs and traces on deploy.",
      "Run `npm run site` and `npm run bundle` locally and COMMIT their output.",
      "That now includes styles.min.css / blog.min.css / script.min.js — the files the",
      "browser actually loads. `npm run site` regenerates them; scripts/assets.test.js",
      "fails if a committed .min file is stale relative to its source."
    ]
  },
  "scripts": {
    "dev": "node scripts/dev.js",
    "site": "node scripts/i18n.js build && node build.js && node scripts/minify-assets.js && node scripts/compile-pages.js",
    "i18n": "node scripts/i18n.js extract",
    "i18n:check": "node scripts/i18n.js check",
    "assets": "node scripts/minify-assets.js",
    "bundle": "node scripts/bundle.js",
    "favicon": "node scripts/gen-favicon.js",
    "watch": "node scripts/bundle.js --watch",
    "test": "node --test \"scripts/*.test.js\"",
    "db:indexes": "node scripts/ensure-indexes.js",
    "check": "npm run test && npm run site && npm run bundle"
  },
  "dependencies": {
    "@vercel/edge": "^1.3.1",
    "cloudinary": "^2.5.1",
    "mongodb": "^6.12.0",
    "nodemailer": "^9.0.4",
    "sanitize-html": "2.13.1",
    "zod": "^3.24.1"
  },
  "overrides": {
    "htmlparser2": "^8.0.2"
  },
  "devDependencies": {
    "@tiptap/core": "^2.10.3",
    "@tiptap/extension-image": "^2.10.3",
    "@tiptap/extension-link": "^2.10.3",
    "@tiptap/extension-underline": "^2.10.3",
    "@tiptap/pm": "^2.10.3",
    "@tiptap/starter-kit": "^2.10.3",
    "esbuild": "^0.24.0",
    "mongodb-memory-server": "^11.2.0",
    "parse5": "^8.0.1"
  }
}
